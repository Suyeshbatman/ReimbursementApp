import { Routes } from '@angular/router';
import { ReceiptFormComponent } from './receipt-form/receipt-form.component';

export const routes: Routes = [
  { path: '', component: ReceiptFormComponent } 
];
